# nSpec Test Workspace

This folder is a committed fixture workspace used by:

- `npm run test:extension`
- VS Code launch config: **Run Extension**

It intentionally stays minimal so extension-host tests always have a valid workspace path.
